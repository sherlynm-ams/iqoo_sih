<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme only; all real theming is done in Compose (ui/theme/Theme.kt). -->
    <style name="Theme.CrossCheck" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
